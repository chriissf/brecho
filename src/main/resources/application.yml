server:
  port: 8180

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/brecho
    username: christian
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
